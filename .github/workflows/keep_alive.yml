name: Keep-Alive
on:
  schedule:
    - cron: '*/14 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping service
        run: curl -s https://vis-sol-offerflow.onrender.com/health | jq .
